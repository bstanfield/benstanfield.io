extends _layout

block content
    .container
        +toast()
        +index_intro()

        +anchor('readings')
        h1 What I&#8217;ve been reading
        br
        
        +books()

        br
        br
       
        +anchor('experiences')
        h1 Experiences
        br
            
        +experience_blocks(experiences.slice(0, 3))
        +experience_blocks(experiences.slice(3))

        br 
        br
        h1 <span class="pulse">💓</span> Pulse
        p.os What I’ve been up to recently... check back in every once in a while for a new Pulse update!
        br

    .grid-bg.grid.long
        +pulse(pulses, 2)

    br 
    br
    br